apiVersion: v1
kind: Service
metadata:
  name: awsdemo-service
  namespace: default
  labels:
    app: awsdemo
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: awsdemo
